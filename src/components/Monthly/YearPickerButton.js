import React from 'react';
import PropTypes from 'prop-types';
import styled from 'styled-components';

class YearPickerButton extends React.Component {

static propTypes = {
    dateForPicker: PropTypes.instanceOf(Date)
  };

  state = {
    open: null
  };

  handleClick = () => {
  };

  render() {
    return ();
  }
}
